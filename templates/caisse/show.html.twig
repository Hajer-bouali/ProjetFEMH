{% extends 'base.html.twig' %}

{% block title %}Caisse{% endblock %}

{% block body %}
    <h1>Caisse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ caisse.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ caisse.intitule }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ caisse.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('caisse_index') }}">back to list</a>

    <a href="{{ path('caisse_edit', {'id': caisse.id}) }}">edit</a>

    {{ include('caisse/_delete_form.html.twig') }}
{% endblock %}
