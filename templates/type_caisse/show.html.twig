{% extends 'base.html.twig' %}

{% block title %}TypeCaisse{% endblock %}

{% block body %}
    <h1>TypeCaisse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_caisse.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_caisse.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_caisse_index') }}">back to list</a>

    <a href="{{ path('type_caisse_edit', {'id': type_caisse.id}) }}">edit</a>

    {{ include('type_caisse/_delete_form.html.twig') }}
{% endblock %}
