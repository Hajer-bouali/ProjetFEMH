{% extends 'base.html.twig' %}

{% block title %}FicheTechnique index{% endblock %}

{% block body %}
    <h1>FicheTechnique index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantite</th>
                <th>Unite</th>
                <th>Nbstockproduit</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fiche_technique in fiche_techniques %}
            <tr>
                <td>{{ fiche_technique.id }}</td>
                <td>{{ fiche_technique.quantite }}</td>
                <td>{{ fiche_technique.unite }}</td>
                <td>{{ fiche_technique.nbstockproduit }}</td>
                <td>
                    <a href="{{ path('app_fiche_technique_show', {'id': fiche_technique.id}) }}">show</a>
                    <a href="{{ path('app_fiche_technique_edit', {'id': fiche_technique.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_fiche_technique_new') }}">Create new</a>
{% endblock %}
