{% extends 'base.html.twig' %}

{% block title %}Typeadherent{% endblock %}

{% block body %}
    <h1>Typeadherent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeadherent.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ typeadherent.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_typeadherent_index') }}">back to list</a>

    <a href="{{ path('app_typeadherent_edit', {'id': typeadherent.id}) }}">edit</a>

    {{ include('typeadherent/_delete_form.html.twig') }}
{% endblock %}
