<form method="post" action="{{ path('app_typeadherent_delete', {'id': typeadherent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typeadherent.id) }}">
    <button class="btn">Delete</button>
</form>
