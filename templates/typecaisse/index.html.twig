{% extends 'base.html.twig' %}

{% block title %}Typecaisse index{% endblock %}

{% block body %}
    <h1>Typecaisse index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for typecaisse in typecaisses|reverse  %}
            <tr>
                <td>{{ typecaisse.id }}</td>
                <td>{{ typecaisse.nom }}</td>
                <td>
                    <a href="{{ path('typecaisse_show', {'id': typecaisse.id}) }}">show</a>
                    <a href="{{ path('typecaisse_edit', {'id': typecaisse.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('typecaisse_new') }}">Create new</a>
{% endblock %}
