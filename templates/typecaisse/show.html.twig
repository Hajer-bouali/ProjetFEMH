{% extends 'base.html.twig' %}

{% block title %}Typecaisse{% endblock %}

{% block body %}
    <h1>Typecaisse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typecaisse.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ typecaisse.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('typecaisse_index') }}">back to list</a>

    <a href="{{ path('typecaisse_edit', {'id': typecaisse.id}) }}">edit</a>

    {{ include('typecaisse/_delete_form.html.twig') }}
{% endblock %}
