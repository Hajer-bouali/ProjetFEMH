<form method="post" action="{{ path('typecaisse_delete', {'id': typecaisse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typecaisse.id) }}">
    <button class="btn">Delete</button>
</form>
