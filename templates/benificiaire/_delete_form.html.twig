<form method="post" action="{{ path('app_benificiaire_delete', {'id': benificiaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ benificiaire.id) }}">
    <button class="btn">Delete</button>
</form>
