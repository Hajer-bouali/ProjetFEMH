{% extends 'base.html.twig' %}

{% block title %}OperationStock{% endblock %}

{% block body %}
    <h1>OperationStock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ operation_stock.id }}</td>
            </tr>
            <tr>
                <th>Datelimitezakat</th>
                <td>{{ operation_stock.datelimitezakat ? operation_stock.datelimitezakat|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ operation_stock.date ? operation_stock.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Responsable</th>
                <td>{{ operation_stock.responsable }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ operation_stock.etat }}</td>
            </tr>
            <tr>
                <th>Nomdonataire</th>
                <td>{{ operation_stock.nomdonataire }}</td>
            </tr>
            <tr>
                <th>Stipulation</th>
                <td>{{ operation_stock.stipulation }}</td>
            </tr>
            <tr>
                <th>Typeoperation</th>
                <td>{{ operation_stock.typeoperation }}</td>
            </tr>
            <tr>
                <th>Unite</th>
                <td>{{ operation_stock.unite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('operation_stock_index') }}">back to list</a>

    <a href="{{ path('operation_stock_edit', {'id': operation_stock.id}) }}">edit</a>

    {{ include('operation_stock/_delete_form.html.twig') }}
{% endblock %}
